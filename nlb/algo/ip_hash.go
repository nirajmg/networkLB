package main

import (
	//"fmt"
	"strings"
	"container/list"
)

type Ip_Hash struct{
	func hash(ip string, n int)(string,error){
		//parse string by .
		//convert string to ints 
		//add ints
		// % by n
		//lookup corresponding ips]
	}

	func Get_IP(ips []string)(string, error){
		//
	}
}
